#!/usr/bin/env ruby
# encoding: utf-8

require 'g_stat'

def help_message
  'Usage: g_stat OWNER REPO'
end

if __FILE__ == $PROGRAM_NAME
  if ARGV.length != 2
    abort help_message
  end

  GStat.run ARGV[0], ARGV[1]
else
  abort 'ERROR: try executing this file.'
end

